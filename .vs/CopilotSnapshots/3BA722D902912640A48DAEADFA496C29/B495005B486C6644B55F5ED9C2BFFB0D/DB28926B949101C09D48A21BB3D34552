namespace NumbersGame
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int secretNumber = new Random().Next(0, 21);
            int attempts = 5;

            Console.WriteLine("Välommen! Jag tänker på ett nummer, kan du gissa vilket? Du får 5 försök");

            int guess = Console.ReadLine()


            if (guess < secretNumber)
            {
                Console.WriteLine("Gissa högre");
            }
            else if (guess > secretNumber)
            {
                Console.WriteLine("Gissa lägre");
            }
            else
            {
                Console.WriteLine("Grattis! Du gissade rätt.");
            }

        }
    }
}
